{
  "name": "agent-browser-bridge",
  "version": "0.2.0",
  "description": "Agent Browser Bridge - Chrome 扩展 + 本地桥（Native Messaging / WebSocket）",
  "private": true,
  "scripts": {
    "start": "node host.js --standalone",
    "install-host": "bash install-host.sh",
    "icons": "node generate-icons.js",
    "test": "node test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
